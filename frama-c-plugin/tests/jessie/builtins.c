
// @ lemma sqr_2: \sqr(2) == 4 && \sqr(2.0) == 4.0;

//@ lemma sqrt_4: \sqrt(4.0) == 2.0;

//@ lemma exp_log_2: \exp(\log(2.0)) == 2.0;

// @ lemma cos_sin: \forall real x; \sqr(\cos(x)) + \sqr(\sin(x)) == 1.0;
 

//@ lemma cos_pi: \cos(\pi) == -1.0;
 

