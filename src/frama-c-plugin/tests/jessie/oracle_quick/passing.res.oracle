rm -f passing.jc
../../bin/toplevel.opt -jessie-analysis  -jessie-gen-only passing.c
Parsing
Cleaning unused parts
Symbolic link
Starting semantical analysis
cat passing.jc
# IntModel = bounded
# InvariantPolicy = Arguments

type _bool = 0..1

type int8 = -128..127

type uint8 = 0..255

type int32 = -2147483648..2147483647

type uint32 = 0..4294967295

type int16 = -32768..32767

type uint16 = 0..65535

type int64 = -9223372036854775808..9223372036854775807

type uint64 = 0..18446744073709551615

tag char_P = {
  int8 char_M;
}

type char_P = [char_P]

tag int_P = {
  int32 int_M;
}

type int_P = [int_P]

tag void_P = {
  unit void_M;
}

type void_P = [void_P]

invariant valid_t :
((\offset_min(t) <= 0) && (\offset_max(t) >= 1))

logic int32 strlen{L}(char_P[..] s) reads s.char_M;

axiom strlen_def1{L} :
(\forall char_P[..] s;
  (\forall int32 i;
    (((0 <= i) && (i < strlen{L}(s))) ==> ((s + i).char_M != 0))))

axiom strlen_def2{L} :
(\forall char_P[..] s;
  ((s + strlen{L}(s)).char_M == 0))

axiom strlen_not_zero{L} :
(\forall char_P[..] s;
  (\forall int32 i;
    ((((0 <= i) && (i <= strlen{L}(s))) && ((s + i).char_M != 0)) ==>
      (i < strlen{L}(s)))))

axiom strlen_zero{L} :
(\forall char_P[..] s;
  (\forall int32 i;
    ((((0 <= i) && (i <= strlen{L}(s))) && ((s + i).char_M == 0)) ==>
      (i == strlen{L}(s)))))

axiom strlen_sup{L} :
(\forall char_P[..] s;
  (\forall int32 i;
    (((0 <= i) && ((s + i).char_M == 0)) ==>
      ((0 <= strlen{L}(s)) && (strlen{L}(s) <= i)))))

axiom strlen_shift{L} :
(\forall char_P[..] s;
  (\forall int32 i;
    (((0 <= i) && (i <= strlen{L}(s))) ==>
      (strlen{L}((s + i)) == (strlen{L}(s) - i)))))

axiom strlen_create{L} :
(\forall char_P[..] s;
  (\forall int32 i;
    (((0 <= i) && ((s + i).char_M == 0)) ==>
      ((0 <= strlen{L}(s)) && (strlen{L}(s) <= i)))))

axiom strlen_create2{L} :
(\forall char_P[..] s;
  (\forall int32 i;
    (\forall int32 k;
      (((k <= i) && ((s + i).char_M == 0)) ==>
        ((0 <= strlen{L}((s + k))) && (strlen{L}((s + k)) <= (i - k)))))))

unit g(int_P[..] x)
  requires (C_3 : ((C_4 : (\offset_min(x) <= 0)) &&
                    (C_5 : (\offset_max(x) >= 0))));
behavior default:
  assumes true;
  assigns x.int_M;
  ensures (C_6 : (x.int_M == 0));
{  
   {  (C_2 : ((C_1 : x.int_M) = 0));
      
      (return ())
   }
}

int_P[..] r;

int32 g2()
  requires (C_10 : ((C_11 : (\offset_min(r) <= 0)) &&
                     (C_12 : (\offset_max(r) >= 0))));
behavior default_1:
  assumes true;
  ensures (C_13 : (\result == 0));
{  
   (var int32 __retres);
   
   {  (C_7 : g(r));
      (C_9 : (__retres = (C_8 : r.int_M)));
      
      (return __retres)
   }
}

unit f(int_P[..] x)
  requires (C_17 : ((C_18 : (\offset_min(x) <= 0)) &&
                     (C_19 : (\offset_max(x) >= 0))));
behavior default_2:
  assumes true;
  assigns (x + 0).int_M;
  ensures (C_20 : ((x + 0).int_M == 1));
{  
   {  (C_16 : ((C_15 : (C_14 : (x + 0)).int_M) = 1));
      
      (return ())
   }
}

int_P[0..1] t;

unit main()
behavior default_3:
  assumes true;
  ensures (C_23 : ((t + 0).int_M == 1));
{  
   {  (C_21 : __globinit_whole_program());
      (C_22 : f(t));
      
      (return ())
   }
}

unit __globinit_whole_program()
{  
   {  (C_25 : (t = (C_24 : (new int_P[2]))))
   }
}
