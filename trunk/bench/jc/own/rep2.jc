type B = {
  integer i;
  boolean owned;
}

type A = {
  rep B[0] b;
  invariant repA(this) = this.b.owned == true;
}

unit m(A[0] a1, A[0] a2) {
  a1.b = a2.b; /* problème si b est rep */
}

/* problème pour définir un champ owner (quel type ?)
   utilisation de owned */