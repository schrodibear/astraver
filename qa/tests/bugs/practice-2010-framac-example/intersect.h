
/*
 * Функция intersect получает на вход два множества чисел set1 и set2 
 * и записывает их пересечение в заранее выделенный массив result.
 * Мощность полученного пересечения возвращается в качестве результата
 * функции.
 */
unsigned intersect(int* set1, unsigned n1, int* set2, unsigned n2, int* result);
