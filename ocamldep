#!/bin/bash

RESULT=$(ocamlfind ocamldep $@)
STATUS=$?
PREFIXED=""
if [[ "$RESULT" =~ src/jc/jc_.*\.mli?:.* ]]; then
    for i in $RESULT; do
        [[ "$i" =~ .*: || "$i" =~ Why_.* ]] || PREFIXED="$PREFIXED Jc_${i,}"
    done
fi
echo $RESULT $PREFIXED
exit $STATUS
