
WHY=../../bin/why.static
WHYOPTIONS=--valid

%_why.v: %.ml $(WHY)
	$(WHY) $(WHYOPTIONS) $<

%.vo: %.v
	coqc -I ../../lib/coq $<

clean::
	rm -f *~ *.vo

depend::
	coqdep -I ../../lib/coq *.v > .depend

include .depend
