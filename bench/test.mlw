

parameter g : 'a -> 'a

let f =
 fun (mode : bool) ->
   let v = ref 0 in
     if mode then
       v := let tmp = g 0 in tmp
     else
       v := let tmp = g 1 in tmp


(*

  $ why -fast-wp test_fastwp.why
  Fatal error: exception Assert_failure("src/fastwp.ml", 129, 17)

*)
