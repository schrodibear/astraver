

/*

struct T {
  int ok;
  int* pt;
  int tab[10];
  //@ invariant pt_validity(t) { t.ok => \valid(t.pt) }
  /*@ invariant tab_nonnegative(t) { 
	\forall int i; 0 <= i && i < 10 => t[i] >=0 
	}	
  */
}


*/


type A = {
  integer x;
  invariant pos(this) = this.x > 0;
}

integer f(A[0] a) 
{
  return 100/a.x;
}

integer g(A[0] a)
{ 
  a.x -= 1;
  integer r = f(a);
  a.x += 1; 
}
