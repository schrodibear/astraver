
# AnnotationPolicy = Invariants
# AbstractDomain = Box

/* 
   @OPTIONS: -main main 
 */


integer f(integer a, integer b) {
     return a / b;
}

integer g(integer a, integer b) {
     return f(a, b);	
}

unit main() {
     integer i = g(10, 2);
}
