
type intP = {
    integer intM;
}

unit f(intP[0] i, intP[0] j) {
    assert (0 < j.intM);
    assert (0 > i.intM);
    i = j;
    assert (i.intM > 0);
    assert (j.intM > 0);
}


/*
Local Variables: 
mode: java
compile-command: "make prop_terms"
End: 
*/
