
# AnnotationPolicy = Invariants
# AbstractDomain = Box

/* 
@OPTIONS: -main main 
*/


type intM = { integer val; }


unit f(intM[0..] t, integer offset) {
    integer i = offset;
    while (i <= 4) variant 4 - i; {
        (t + i).val = 0;
	i++;
    }
}

unit main() {
   intM[0..] t1 = new intM[5];
   f(t1, 0);
   intM[0..] t2 = new intM[10];
   f(t2, 2);
}

/*
Local Variables: 
mode: java
compile-command: "make ai_inter2"
End: 
*/
