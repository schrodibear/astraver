
# AnnotationPolicy = Invariants
# AbstractDomain = Box

logic pos(integer x) = x > 0


unit f(integer n) 
  requires pos(n); 
{
   var integer i = n;
   variant 10 - i; 
   while (i < 10) {
       var integer j = 1 / i;
       i++;
   }
}

unit f_bis(integer n) 
  requires n > 0; 
{
   var integer i = n;
   variant 10 - i; 
   while (i < 10) {
       var integer j = 1 / i;
       i++;
   }
}
