type Point = {
  integer x;
  integer y;
}

type ColorPoint = Point with { integer color; }

unit f()
  requires true;
{
  (let x = (new Point[0]) in x).x = 0;
} 

/*
Local Variables: 
compile-command: "make test"
End: 
*/
