(* This file was originally generated by why.
   It can be modified; only the generated parts will be overwritten. *)

Require Export unsafe_spec_why.

(* Why obligation from file "", line 0, characters 0-0: *)
(*Why goal*) Lemma f_impl_po_1 : 
  forall (i: Z),
  forall (alloc: alloc_table),
  forall (t: ((pointer) Z0)),
  forall (HW_1: (valid_range alloc t 0 9)),
  forall (result: ((pointer) Z0)),
  forall (HW_2: result = (shift t (i - i))),
  (valid alloc result).
Proof.
intuition.
(* FILL PROOF HERE *)
Save.

(* Why obligation from file "", line 0, characters 0-0: *)
(*Why goal*) Lemma g_impl_po_1 : 
  forall (alloc: alloc_table),
  forall (t: ((pointer) Z0)),
  forall (HW_1: (valid_range alloc t 0 9)),
  forall (result: ((pointer) Z0)),
  forall (HW_2: result = (shift t 10)),
  (valid alloc result).
Proof.
intuition.
(* FILL PROOF HERE *)
Save.

(* Why obligation from file "", line 0, characters 0-0: *)
(*Why goal*) Lemma h_impl_po_1 : 
  forall (i: Z),
  forall (alloc: alloc_table),
  forall (t: ((pointer) Z0)),
  forall (HW_1: (valid_range alloc t 0 9)),
  forall (result: ((pointer) Z0)),
  forall (HW_2: result = (shift t i)),
  (valid alloc result).
Proof.
intuition.
(* FILL PROOF HERE *)
Save.

