(* This file was originally generated by why.
   It can be modified; only the generated parts will be overwritten. *)

Require Import Why.


Proof.
intuition.
Qed.

Proof.
intuition.
Qed.

Proof.
intuition.
discriminate H0.
Qed.



(* Why obligation from file "why/dowhile.why", characters 172-176 *)
Lemma main_impl_po_1 : 
  forall (x: Z),
  forall (Pre4: x >= 0),
  forall (x0: Z),
  forall (Post1: x0 = 0),
  forall (i0: Z),
  forall (Post2: i0 = 10),
  forall (Variant1: Z),
  forall (i1: Z),
  forall (x1: Z),
  forall (Pre3: Variant1 = i1),
  forall (Pre2: x1 = (10 - i1) /\ 10 >= i1 /\ i1 > 0),
  (forall (x:Z),
   (x = (x1 + 1) ->
    (forall (i:Z),
     (i = (i1 - 1) ->
      ((i > 0 -> (x = (10 - i) /\ 10 >= i /\ i > 0) /\ (Zwf 0 i i1))) /\
      ((i <= 0 -> x = 10)))))).
Proof.
intuition.
(* FILL PROOF HERE *)
Save.

(* Why obligation from file "why/dowhile.why", characters 208-272 *)
Lemma main_impl_po_2 : 
  forall (x: Z),
  forall (Pre4: x >= 0),
  forall (x0: Z),
  forall (Post1: x0 = 0),
  forall (i0: Z),
  forall (Post2: i0 = 10),
  x0 = (10 - i0) /\ 10 >= i0 /\ i0 > 0.
Proof.
intuition.
(* FILL PROOF HERE *)
Save.

Proof.
intuition.
(* FILL PROOF HERE *)
Save.

Proof.
intuition.
(* FILL PROOF HERE *)
Save.

Proof.
intuition.
(* FILL PROOF HERE *)
Save.

Proof.
intuition.
(* FILL PROOF HERE *)
Save.

