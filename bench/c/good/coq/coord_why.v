(* This file was originally generated by why.
   It can be modified; only the generated parts will be overwritten. *)

Require Export caduceus_spec_why.

(* Why obligation from file "why/coord.why", characters 89-121 *)
Lemma g_impl_po_1 : 
  forall (index: Z),
  forall (alloc: alloc_table),
  forall (tab: pointer),
  forall (aux_1: pointer),
  forall (Post2: aux_1 = (shift tab index)),
  (valid alloc aux_1).
Proof.
intuition.
(* FILL PROOF HERE *)
Save.

(* Why obligation from file "why/coord.why", characters 89-121 *)
Lemma g_impl_po_2 : 
  forall (index: Z),
  forall (alloc: alloc_table),
  forall (tab: pointer),
  forall (x: ((memory) Z)),
  forall (aux_1: pointer),
  forall (Post2: aux_1 = (shift tab index)),
  forall (Pre1: (valid alloc aux_1)),
  forall (result: Z),
  forall (Post4: result = (acc x aux_1)),
  (True ->
   (forall (result:pointer),
    (result = (shift tab index) ->
     (forall (result0:Z), (result0 = (acc x result) -> (True -> True))) /\
     (valid alloc result)))).
Proof.
intuition.
(* FILL PROOF HERE *)
Save.

